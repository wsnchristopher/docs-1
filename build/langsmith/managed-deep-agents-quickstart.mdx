---
title: Managed Deep Agents quickstart
sidebarTitle: Quickstart
description: Create and deploy your first Managed Deep Agent with the CLI.
---

import ManagedDeepAgentsPrivateBetaNote from '/snippets/langsmith/managed-deep-agents-private-beta-note.mdx';

Deploy a hosted Deep Agent without setting up infrastructure. LangChain hosts [Managed Deep Agents](/langsmith/managed-deep-agents-overview) for you. This quickstart scaffolds a local project, edits the agent instructions, and deploys a research assistant that searches sources and returns cited answers.

For the full deploy workflow and all backend options, see [Deploy an agent](/langsmith/managed-deep-agents-deploy).

<Note>
<ManagedDeepAgentsPrivateBetaNote />
</Note>

## Prerequisites

Before you start, make sure you have:

- A workspace with Managed Deep Agents [private beta access](https://www.langchain.com/langsmith-managed-deep-agents-waitlist).
- A [LangSmith API key](/langsmith/create-account-api-key) for that workspace.
- The `deepagents-cli>=0.2.2` package.

## Create and deploy an agent

<Steps>
  <Step title="Install the CLI" id="install-the-cli">

<Note>
Managed Deep Agents requires `deepagents-cli>=0.2.2`.
</Note>

Install `deepagents-cli` with `uv` (preferred) or `pip`:

<CodeGroup>

```bash uv
uv tool install "deepagents-cli>=0.2.2"
```

```bash pip
pip install -U "deepagents-cli>=0.2.2"
```

</CodeGroup>

To upgrade an existing CLI installation, run `uv tool upgrade deepagents-cli` (or `pip install -U "deepagents-cli"` if you installed with pip).

  </Step>

  <Step title="Set your API key" id="set-your-api-key">

Set the LangSmith API key for your beta-enabled workspace:

```bash
export LANGSMITH_API_KEY="<LANGSMITH_API_KEY>"
```

The CLI targets US LangSmith Cloud by default.

<Note>
If a request returns 401 or 403, confirm the key belongs to a workspace with beta access.
</Note>

  </Step>

  <Step title="Create a project" id="create-a-project">

Create a Managed Deep Agents project:

```bash
deepagents init research-assistant
cd research-assistant
```

The CLI creates:

| File or directory | Purpose |
| --- | --- |
| `agent.json` | Agent metadata: model, backend, and an optional `agent_id` for [redeploying to an existing agent](/langsmith/managed-deep-agents-cli#target-existing-agents). |
| `AGENTS.md` | Main agent instructions. |
| `tools.json` | Empty MCP tool configuration. |
| `skills/example-skill/` | Example skill you can edit or remove. |
| `subagents/researcher/` | Example subagent you can edit or remove. |
| `.gitignore` | Excludes local environment files. |

  </Step>

  <Step title="Edit the instructions" id="edit-the-instructions">

Open `AGENTS.md` and replace the generated instructions with the behavior you want:

```markdown AGENTS.md
# Research assistant

You are a careful research assistant. Search for sources, keep notes, and return
concise answers with citations.
```

The generated project uses the `state` backend so the agent can deploy without sandbox-specific configuration. Switch to the `sandbox` backend when the agent needs a [LangSmith sandbox](/langsmith/sandboxes) for code execution, filesystem work, or long-running tasks. For options, see [Choose a backend](/langsmith/managed-deep-agents-deploy#choose-a-backend).

If the agent calls MCP tools, [connect tools](/langsmith/managed-deep-agents-mcp) before deploying the agent.

  </Step>

  <Step title="Deploy the agent" id="deploy-the-agent">

Deploy the local project:

```bash
deepagents deploy
```

<Note>
Free LangSmith workspaces are limited to one Managed Deep Agent. If `deepagents deploy` fails because the workspace is at its limit, delete the existing agent or upgrade the plan.
</Note>

On success, the CLI prints the agent name, ID, short revision, agent URL, and a post-deploy MCP health check:

```text
Deployed: research-assistant
  agent_id: e2de7a35-9dda-462b-b982-9e57051993bc
  revision: 13ac11f1
  https://smith.langchain.com/o/-/agents/e2de7a35-9dda-462b-b982-9e57051993bc
  health:   {'agent_id': '...', 'mcp_check': {'ok': True, 'servers': []}, ...}
```

The `health` field reports the post-deploy MCP check. `ok: True` means the agent's tools resolved, and an empty `servers` list is expected until you [connect tools](/langsmith/managed-deep-agents-mcp).

Open the printed agent URL in LangSmith to inspect traces and revisions for the deployed agent.

  </Step>
</Steps>

## Next steps

<CardGroup cols={2}>
  <Card title="Connect tools" icon="plug" href="/langsmith/managed-deep-agents-mcp">
    Register MCP servers and reference their tools from `tools.json`.
  </Card>
  <Card title="Deploy an agent" icon="upload" href="/langsmith/managed-deep-agents-deploy">
    Walk through the full deploy workflow and project file layout.
  </Card>
  <Card title="CLI reference" icon="terminal" href="/langsmith/managed-deep-agents-cli">
    Look up every command, flag, and validation rule.
  </Card>
  <Card title="LangSmith sandboxes" icon="box" href="/langsmith/sandboxes">
    Give agents isolated environments for code execution and filesystem work.
  </Card>
</CardGroup>

---

<div className="source-links">
<Callout icon="terminal-2">
    [Connect these docs](/use-these-docs) to Claude, VSCode, and more via MCP for real-time answers.
</Callout>
<Callout icon="edit">
    [Edit this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/langsmith/managed-deep-agents-quickstart.mdx) or [file an issue](https://github.com/langchain-ai/docs/issues/new/choose).
</Callout>
</div>
