<CodeGroup>
    ```python Google
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="google_genai:gemini-3.5-flash"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python OpenAI
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="openai:gpt-5.5"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python Anthropic
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="anthropic:claude-sonnet-4-6"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python OpenRouter
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="openrouter:z-ai/glm-5.2"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python Fireworks
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="fireworks:accounts/fireworks/models/kimi-k2p7-code"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python Baseten
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="baseten:zai-org/GLM-5.2"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```

    ```python Ollama
    from langchain_core.vectorstores import InMemoryVectorStore
    from langchain_openai import OpenAIEmbeddings
    
    vector_store = InMemoryVectorStore(OpenAIEmbeddings(model="ollama:north-mini-code-1.0"))
    document_ids = vector_store.add_documents(documents=all_splits)
    
    print(document_ids[:3])
    ```
</CodeGroup>
