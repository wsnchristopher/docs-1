```python
from langchain.messages import HumanMessage

if __name__ == "__main__":
    result = agent.invoke(
        {
            "messages": [
                HumanMessage(
                    content=(
                        "How do I add a retrieval tool to a LangChain agent, "
                        "and when should I use subagents instead of putting all "
                        "retrieved context in the main prompt?"
                    )
                )
            ]
        }
    )

    for msg in result.get("messages", []):
        if hasattr(msg, "content") and msg.content:
            print(msg.content)
```
