```ts
import { HumanMessage } from "@langchain/core/messages";

if (import.meta.main) {
  const result = await agent.invoke({
    messages: [
      new HumanMessage(
        "How do I add a retrieval tool to a LangChain agent, and when should I use subagents instead of putting all retrieved context in the main prompt?",
      ),
    ],
  });

  for (const msg of result.messages ?? []) {
    if (msg.content) {
      console.log(msg.content);
    }
  }
}
```
