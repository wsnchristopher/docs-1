---
title: The agent development lifecycle
sidebarTitle: Agent lifecycle
description: Build, test, deploy, monitor, and govern an agent end to end using Deep Agents and LangSmith.
mode: wide
---

This walkthrough follows one agent through every stage of the agent development lifecycle: build, test, deploy, monitor, and govern. It uses [Deep Agents](/oss/deepagents/overview) as the example framework, but each stage is framework-agnostic. Bring your own stack and adopt only the stages you need, or use LangSmith as the all-in-one platform.

<Note>
This page is a scaffold. Code samples are placeholders to be filled in and validated against `deepagents>=0.6.1` before publishing.
</Note>

## The lifecycle

{/* TODO: replace with the cyclical lifecycle SVG (Design). Stages: Build → Test → Deploy → Monitor → Govern, with Engine at the center. */}

<CardGroup cols={3}>
  <Card title="Build" icon="hammer" href="/oss/deepagents/overview" />
  <Card title="Test" icon="flask" href="/langsmith/evaluation-concepts" />
  <Card title="Deploy" icon="rocket" href="/langsmith/deployment-quickstart" />
  <Card title="Monitor" icon="chart-line" href="/langsmith/observability" />
  <Card title="Govern" icon="shield-lock" href="/langsmith/govern" />
  <Card title="Engine" icon="bolt" href="/langsmith/engine" />
</CardGroup>

## Build

Define the agent with Deep Agents. See [Deep Agents](/oss/deepagents/overview).

```python
# TODO: scaffold — create a Deep Agent (validate against deepagents>=0.6.1)
```

## Test

Evaluate the agent against a dataset in LangSmith. See [Evaluation](/langsmith/evaluation-concepts).

```python
# TODO: scaffold — create a dataset and run an evaluation
```

## Deploy

Deploy the agent to the LangSmith agent server. See [Agent deployment](/langsmith/agent-server).

```python
# TODO: scaffold — deploy and invoke the agent
```

## Monitor

Trace and monitor the running agent. See [Observability](/langsmith/observability).

```python
# TODO: scaffold — trace requests and inspect runs
```

## Govern

Enforce model access, spend, and data policies. See [Governance](/langsmith/govern).

```python
# TODO: scaffold — route calls through the LLM Gateway with spend and redaction policies
```

## Engine

{/* TODO: pull in shared Engine snippet once created (connects UI and webhooks across stages). */}

Learn how Engine connects the lifecycle. See [Engine](/langsmith/engine).
